name: 🤖 Prompt Runner GPT-5-mini

on:
  push:
    branches:
      - main
      - master

jobs:
  run-prompt:
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Checkout
        uses: actions/checkout@v4

      - name: ⚙️ Setup environment
        run: |
          set -e
          mkdir -p .debug
          echo "OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}" >> $GITHUB_ENV

      - name: 🧠 Build reduced context
        run: |
          set -e
          echo "🧠 Criando contexto leve..."
          MAX_SIZE=15000
          MAX_TOTAL=180000
          CURRENT_SIZE=0
          CONTEXT_FILE=full_context.txt

          # Se arquivo não existe ou está vazio, gera completo
          if [ ! -f "$CONTEXT_FILE" ] || [ ! -s "$CONTEXT_FILE" ]; then
            echo "" > "$CONTEXT_FILE"
            for F in $(git ls-files); do
              if [[ -f "$F" ]]; then
                SIZE=$(wc -c < "$F")
                if [ "$((CURRENT_SIZE + SIZE))" -gt "$MAX_TOTAL" ]; then
                  break
                fi
                echo "==== $F ====" >> "$CONTEXT_FILE"
                head -c "$MAX_SIZE" "$F" >> "$CONTEXT_FILE"
                echo "\n" >> "$CONTEXT_FILE"
                CURRENT_SIZE=$((CURRENT_SIZE + SIZE))
              fi
            done
          fi

          echo "📦 Contexto final: $(wc -c < "$CONTEXT_FILE") bytes"

      - name: 🧾 Generate AI Response (GPT-5-mini)
        id: ai
        run: |
          set -e
          # Pega o prompt do commit
          COMMIT_MSG=$(git log -1 --pretty=%B | grep '^prompt:' | sed 's/^prompt: //')
          if [ -z "$COMMIT_MSG" ]; then
            echo "⚠️ Nenhum prompt encontrado. Abortando."
            exit 0
          fi
          echo "📝 Prompt detectado: $COMMIT_MSG"

          REQUEST=$(jq -n \
            --arg model "gpt-5-mini-2025-08-07" \
            --arg prompt "$COMMIT_MSG" \
            --arg context "$(head -c 180000 "$CONTEXT_FILE")" \
            '{
              model: $model,
              messages: [
                {role:"system", content:"Você é um assistente de programação Cicada, uma bet casa de apostas, que gera arquivos a partir de prompts de commit."},
                {role:"user", content:($prompt + "\n\nContexto:\n" + $context)}
              ],
              max_completion_tokens: 3000
            }'
          )

          echo "$REQUEST" > .debug/request.json

          RESPONSE=$(curl -s -X POST "https://api.openai.com/v1/chat/completions" \
            -H "Authorization: Bearer $OPENAI_API_KEY" \
            -H "Content-Type: application/json" \
            -d "$REQUEST")

          echo "$RESPONSE" > .debug/response_raw.txt
          jq -r '.choices[0].message.content' <<< "$RESPONSE" > ai_raw.txt
          echo "✅ Resposta bruta salva em ai_raw.txt"

      - name: 💾 Convert AI output to safe JSON
        run: |
          set -e
          AI_CONTENT=$(jq -Rs '.' ai_raw.txt)
          # Gera JSON seguro para aplicação
          echo "{ \"path\": \"README.md\", \"content\": $AI_CONTENT }" > ai_output.json
          echo "✅ JSON seguro gerado em ai_output.json"
          head -n 20 ai_output.json

      - name: 💾 Apply changes and commit
        run: |
          set -e
          if grep -q '"path"' ai_output.json; then
            echo "🧩 Aplicando alterações..."
            jq -c '.' ai_output.json | while read i; do
              FILE=$(echo "$i" | jq -r '.path')
              CONTENT=$(echo "$i" | jq -r '.content')
              mkdir -p "$(dirname "$FILE")"
              echo "$CONTENT" > "$FILE"
              echo "✅ Arquivo atualizado: $FILE"
            done

            git config user.name "prompt-bot"
            git config user.email "actions@github.com"

            if [[ -n "$(git status --porcelain)" ]]; then
              git add .
              git commit -m "🤖 AI applied changes for prompt"
              git push
              echo "✅ Alterações enviadas!"
            else
              echo "⚙️ Nenhuma alteração detectada."
            fi
          else
            echo "⚠️ Nenhum JSON válido detectado. Resposta GPT ignorada."
          fi
